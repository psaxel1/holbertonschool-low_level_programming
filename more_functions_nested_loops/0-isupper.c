#include "main.h"

/**
 * _isupper -check if char is lower case is it return 1
 *
 * @c: is char
 *
 * Return: 1 if upper, 0 if else
 */

int _isupper(int c)
{
	if (isupper(c))
	{
		return (1);
	}
	return (0);
}
