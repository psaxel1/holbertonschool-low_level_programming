#include "main.h"
/**
 * puts_half -print half of array
 *@str: array
 */
void puts_half(char *str)
{
	int i, j;

	for (i = 0; str[i] != '\0'; i++)
	{
	}

	for (j = 0; j < i / 2; j++)
	{
	}
	if (i > 10)
	{
	j++;
	}
	for (; j < i ; j++)
	{
		_putchar (str[j]);
	}

	_putchar ('\n');
}
